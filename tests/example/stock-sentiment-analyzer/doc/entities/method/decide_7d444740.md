---
type: skill
version: '1.0'
category: entities
entity_type: method
entity_id: 7d44474052dd7cd9
signature: ad9fbab7f9be8628eeebd4baf6d76d57
created: '2026-03-11T10:11:01.607293'
file_path: src\trader\short_term_trader.py
start_line: 35
end_line: 74
lines_of_code: 40
tags:
- entity
- method
- decide
related_entities: []
dependencies:
  out: []
  in: []
metrics:
  complexity: null
  param_count: 3
---

# decide

> **类型**: `EntityType.METHOD` | **文件**: `src\trader\short_term_trader.py` | **行数**: 35-74 (40行)

## 📋 概述

**说明**:

```
短线决策

Args:
    news: 资讯信息
    aggregated_result: 汇总结果

Returns:
    决策结果
```

## 📥 参数

| 名称 | 类型 | 说明 |
|------|------|------|
| `self` | `any` | - |
| `news` | `any` | - |
| `aggregated_result` | `any` | - |

## 📤 返回值

**类型**: `Dict[str, Any]`

## 💻 代码实现

```python
    async def decide(self, news: Dict[str, Any], aggregated_result: Dict[str, Any]) -> Dict[str, Any]:
        """短线决策

        Args:
            news: 资讯信息
            aggregated_result: 汇总结果

        Returns:
            决策结果
        """
        self.logger.info(f"开始短线决策: {news.get('title', '')}")

        # 获取汇总结果
        aggregated = aggregated_result.get('aggregated_result', {})
        impact_level = aggregated.get('impact_level', '微影响')
        impact_direction = aggregated.get('impact_direction', '中性')
        affected_sectors = aggregated.get('affected_sectors', [])
        confidence = aggregated.get('confidence', 0.5)

        # 定位受影响的概念
        affected_concepts = self._identify_concepts(news, affected_sectors)

        # 筛选个股
        stock_analysis = self._filter_stocks(
            affected_concepts,
            impact_level,
            impact_direction,
            confidence
        )

        # 构建结果
        result = {
            'news_id': aggregated_result.get('news_id'),
            'news_title': news.get('title', ''),
            'affected_concepts': affected_concepts,
            'stock_analysis': stock_analysis
        }

        self.logger.info(f"短线决策完成: {news.get('title', '')}")
        return result
```

---
## 🤖 AI智能体指南

### 快速定位
- **文件路径**: `src\trader\short_term_trader.py`
- **起始行号**: 35
- **搜索关键词**: `decide`

### 签名追踪
- **签名**: `ad9fbab7f9be8628...`

---
*由知识提取智能体自动生成 | Skill格式 v1.0*
